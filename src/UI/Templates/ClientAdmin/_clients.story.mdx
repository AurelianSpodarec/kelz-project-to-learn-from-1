import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs"
import Container from "@4cplatform/elements/Atoms/Container"
import { H1 } from "@4cplatform/elements/Typography"
import { ConfigProvider } from "@4cplatform/elements/Config"
import { TranslationsProvider } from "@4cplatform/elements/Translations"
import { AlertsProvider, AlertsContainer } from "@4cplatform/elements/Alerts"
import { Page } from "../../Organisms"
import { FakeAuthProvider } from "../../Helpers"
import CONFIG from "../../../config"
import TestClientsProvider from "./story/clients.story.provider"
import ClientAdmin, { ClientsPanel } from "."

<Meta
  title="Templates/ClientAdmin"
  decorators={[
    StoryFn => (
      <ConfigProvider config={CONFIG}>
        <TranslationsProvider>
          <AlertsProvider>
            <FakeAuthProvider>
              <Page>
                <TestClientsProvider>
                  <AlertsContainer />
                  <StoryFn />
                </TestClientsProvider>
              </Page>
            </FakeAuthProvider>
          </AlertsProvider>
        </TranslationsProvider>
      </ConfigProvider>
    )
  ]}
/>

# Client Administration

The `ClientAdmin` component allows you to view clients sold on the platform. It is designed to be used in multiple different contexts, as the platform requires different views for clients in different states, and these require their own pages.

## Usage

Implementing this component is more complicated than usual because there are three components that must be defined and implemented separately in order for it to work. This is because different contexts for `clientAdmin` have different visual design guidelines that must be worked around. Developers therefore need more control over how these components are implemented.

There are three main parts to this component:

1. `ClientAdmin`
2. `ClientsPanel`
3. `ClientsProvider`

`ClientAdmin` contains the `Table` component for displaying the index clients data as well as several tools for sorting and refining the Index clients query.

`ClientsPanel` is a `FlyOutPanel` component that contains the View client and Edit client functionality. Because `FlyOutPanel` is an absolutely positioned element, it must be defined at the root of the page being designed. It is for this reason that a React context is desirable for managing the state of this component.

`ClientsProvider` is the Context Provider. It controls the state of the `ClientAdmin` component and the `ClientsPanel` component. It must be defined outside of the component in order to allow developers to control how the queries and mutations are performed inside the multiple pages that the `ClientAdmin` component is defined within. Different implementations of `ClientAdmin` will have slightly different needs, and using a context to control the state will allow for that.

Although the particular state management technique within the context is left open, it makes sense to use a `useReducer` hook for state management of page actions. For the `EditClient` component, a formik instance internal to the component handles the state of the form inputs.

An example of a functional `ClientsProvider` instance can be found inside `./story/clients.story.provider.js`. Refer to this when considering the bare minimum of fields required to make this component functional.

Implement as follows:

```javascript
<>
  <Container>
    <ClientAdmin {...props} />
  </Container>
  <ClientsPanel {...props} />
</>
```

## Story

export const Template = args => (
  <>
    <Container backgroundColour="#fff">
      <H1 margin="3rem 0">Clients</H1>
      <ClientAdmin {...args} />
    </Container>
    <ClientsPanel {...args} />
  </>
)

<Canvas>
  <Story name="ClientAdmin">{Template.bind()}</Story>
</Canvas>
