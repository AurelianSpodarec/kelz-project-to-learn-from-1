import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs"
import Container from "@4cplatform/elements/Atoms/Container"
import { H1 } from "@4cplatform/elements/Typography"
import { ConfigProvider } from "@4cplatform/elements/Config"
import { TranslationsProvider } from "@4cplatform/elements/Translations"
import { AlertsProvider, AlertsContainer } from "@4cplatform/elements/Alerts"
import { Page } from "../../Organisms"
import { FakeAuthProvider } from "../../Helpers"
import CONFIG from "../../../config"
import TestJourneysProvider from "./story/journeys.story.provider"
import JourneyAdmin, { JourneysPanel } from "."

<Meta
  title="Templates/JourneyAdmin"
  decorators={[
    StoryFn => (
      <ConfigProvider config={CONFIG}>
        <TranslationsProvider>
          <AlertsProvider>
            <FakeAuthProvider>
              <Page>
                <TestJourneysProvider>
                  <AlertsContainer />
                  <StoryFn />
                </TestJourneysProvider>
              </Page>
            </FakeAuthProvider>
          </AlertsProvider>
        </TranslationsProvider>
      </ConfigProvider>
    )
  ]}
/>

# Journey Administration

The `JourneyAdmin` component allows you to view quotes sold on the platform. It is designed to be used in multiple different contexts, as the platform requires different views for quotes in different states, and these require their own pages.

## Usage

Implementing this component is more complicated than usual because there are three components that must be defined and implemented separately in order for it to work. This is because different contexts for `JourneyAdmin` have different visual design guidelines that must be worked around. Developers therefore need more control over how these components are implemented.

There are three main parts to this component:

1. `JourneyAdmin`
2. `JourneysPanel`
3. `JourneysProvider`

`JourneyAdmin` contains the `Table` component for displaying the index quotes data as well as several tools for sorting and refining the Index Journeys query.

`JourneysPanel` is a `FlyOutPanel` component that contains the View Journey and Edit Journey functionality. Because `FlyOutPanel` is an absolutely positioned element, it must be defined at the root of the page being designed. It is for this reason that a React context is desirable for managing the state of this component.

`JourneysProvider` is the Context Provider. It controls the state of the `JourneyAdmin` component and the `JourneysPanel` component. It must be defined outside of the component in order to allow developers to control how the queries and mutations are performed inside the multiple pages that the `JourneyAdmin` component is defined within. Different implementations of `JourneyAdmin` will have slightly different needs, and using a context to control the state will allow for that.

Although the particular state management technique within the context is left open, it makes sense to use a `useReducer` hook for state management of page actions.

An example of a functional `JourneysProvider` instance can be found inside `./story/journeys.story.provider.js`. Refer to this when considering the bare minimum of fields required to make this component functional.

Implement as follows:

```javascript
<>
  <Container>
    <JourneyAdmin {...props} />
  </Container>
  <JourneysPanel {...props} />
</>
```

## Story

export const Template = args => (
  <>
    <Container backgroundColour="#fff">
      <H1 margin="3rem 0">Journeys</H1>
      <JourneyAdmin {...args} />
    </Container>
    <JourneysPanel {...args} />
  </>
)

<Canvas>
  <Story name="JourneyAdmin">{Template.bind()}</Story>
</Canvas>
