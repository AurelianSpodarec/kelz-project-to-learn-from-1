import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs"
import Container from "@4cplatform/elements/Atoms/Container"
import { H1 } from "@4cplatform/elements/Typography"
import { Providers } from "../../../Helpers"
import TestAgencyCodesProvider from "./story/organisationAgencyCodes.story.provider"
import AgencyCodeOrganisationAdmin, { OrganisationAgencyCodesPanel } from "."

<Meta
  title="Templates/Agency Codes/AgencyCodeOrganisationAdmin"
  parameters={{
    layout: "fullscreen",
    jest: ["_organisationAgencyCodes.test.js"]
  }}
  decorators={[
    StoryFn => (
      <Providers>
        <StoryFn />
      </Providers>
    )
  ]}
/>

# Organisation AgencyCode Administration

The `AgencyCodeOrganisationAdmin` component allows you to view agency codes and commision rates on the platform. It is designed to be used in multiple different contexts, as the platform requires different views for agency codes in different states, and these require their own pages.

## Usage

Implementing this component is more complicated than usual because there are three components that must be defined and implemented separately in order for it to work. This is because different contexts for `AgencyCodeOrganisationAdmin` have different visual design guidelines that must be worked around. Developers therefore need more control over how these components are implemented.

There are three main parts to this component:

1. `AgencyCodeOrganisationAdmin`
2. `OrganisationAgencyCodesPanel`
3. `AgencyCodesProvider`

`AgencyCodeOrganisationAdmin` contains the `Table` component for displaying the index agency codes data as well as several tools for sorting and refining the Index agency codes query.

`OrganisationAgencyCodesPanel` is a `FlyOutPanel` component that contains the View agency code and Edit agency code functionality. Because `FlyOutPanel` is an absolutely positioned element, it must be defined at the root of the page being designed. It is for this reason that a React context is desirable for managing the state of this component.

`AgencyCodesProvider` is the Context Provider. It controls the state of the `AgencyCodeOrganisationAdmin` component and the `OrganisationAgencyCodesPanel` component. It must be defined outside of the component in order to allow developers to control how the queries and mutations are performed inside the multiple pages that the `AgencyCodeOrganisationAdmin` component is defined within. Different implementations of `AgencyCodeOrganisationAdmin` will have slightly different needs, and using a context to control the state will allow for that.

Although the particular state management technique within the context is left open, it makes sense to use a `useReducer` hook for state management of page actions.

An example of a functional `AgencyCodesProvider` instance can be found inside `./story/organisationAgencyCodes.story.provider.js`. Refer to this when considering the bare minimum of fields required to make this component functional.

Implement as follows:

```javascript
<>
  <Container>
    <AgencyCodeOrganisationAdmin {...props} />
  </Container>
  <OrganisationAgencyCodesPanel {...props} />
</>
```

## Story

export const Template = args => (
  <TestAgencyCodesProvider {...args}>
    <Container>
      <H1 margin="3rem 0">Organisation agency codes</H1>
      <AgencyCodeOrganisationAdmin />
    </Container>
    <OrganisationAgencyCodesPanel />
  </TestAgencyCodesProvider>
)

<Canvas>
  <Story name="Agency Codes" args={{ value: { type: "default" } }}>
    {Template.bind()}
  </Story>
  <Story name="Shared from Network" args={{ value: { type: "shared_from_network" } }}>
    {Template.bind()}
  </Story>
  <Story name="Shared with Users" args={{ value: { type: "shared_with_users" } }}>
    {Template.bind()}
  </Story>
  <Story name="Assigned to Users" args={{ value: { type: "assigned_to_users" } }}>
    {Template.bind()}
  </Story>
</Canvas>
