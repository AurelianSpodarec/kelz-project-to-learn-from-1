import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs"
import Container from "@4cplatform/elements/Atoms/Container"
import { H1 } from "@4cplatform/elements/Typography"
import { ConfigProvider } from "@4cplatform/elements/Config"
import { TranslationsProvider } from "@4cplatform/elements/Translations"
import { AlertsProvider, AlertsContainer } from "@4cplatform/elements/Alerts"
import { Page } from "../../../Organisms"
import { FakeAuthProvider } from "../../../Helpers"
import CONFIG from "../../../../config"
import TestAgencyCodeRequestsProvider from "./story/codeRequests.story.provider"
import AgencyCodeRequestAdmin from "."

<Meta
  title="Templates/Agency Codes/AgencyCodeRequestAdmin"
  parameters={{
    layout: "fullscreen",
    jest: ["_codeRequests.test.js"]
  }}
  decorators={[
    StoryFn => (
      <Providers>
        <TestAgencyCodeRequestsProvider>
          <StoryFn />
        </TestAgencyCodeRequestsProvider>
      </Providers>
    )
  ]}
/>

# AgencyCodeRequest Administration

The `AgencyCodeRequestAdmin` component allows you to view agency codes and commision rates on the platform. It is designed to be used in multiple different contexts, as the platform requires different views for clients in different states, and these require their own pages.

## Usage

Implementing this component is more complicated than usual because there are three components that must be defined and implemented separately in order for it to work. This is because different contexts for `AgencyCodeRequestAdmin` have different visual design guidelines that must be worked around. Developers therefore need more control over how these components are implemented.

There are three main parts to this component:

1. `AgencyCodeRequestAdmin`
2. `AgencyCodeRequestsPanel`
3. `AgencyCodeRequestsProvider`

`AgencyCodeRequestAdmin` contains the `Table` component for displaying the index agency codes requests data as well as several tools for sorting and refining the Index agency codes requests query.

`AgencyCodeRequestsPanel` is a `FlyOutPanel` component that contains the View agency code request. Because `FlyOutPanel` is an absolutely positioned element, it must be defined at the root of the page being designed. It is for this reason that a React context is desirable for managing the state of this component.

`AgencyCodeRequestsProvider` is the Context Provider. It controls the state of the `AgencyCodeRequestAdmin` component and the `AgencyCodeRequestsPanel` component. It must be defined outside of the component in order to allow developers to control how the queries and mutations are performed inside the multiple pages that the `AgencyCodeRequestAdmin` component is defined within. Different implementations of `AgencyCodeRequestAdmin` will have slightly different needs, and using a context to control the state will allow for that.

Although the particular state management technique within the context is left open, it makes sense to use a `useReducer` hook for state management of page actions.

An example of a functional `AgencyCodeRequestsProvider` instance can be found inside `./story/codeRequests.story.provider.js`. Refer to this when considering the bare minimum of fields required to make this component functional.

Implement as follows:

```javascript
<>
  <Container>
    <AgencyCodeRequestAdmin {...props} />
  </Container>
  <AgencyCodeRequestsPanel {...props} />
</>
```

## Story

export const Template = args => (
  <>
    <Container backgroundColour="#fff">
      <H1 margin="3rem 0">Agency codes</H1>
      <AgencyCodeRequestAdmin {...args} />
    </Container>
    <AgencyCodeRequestsPanel {...args} />
  </>
)

<Canvas>
  <Story name="AgencyCodeRequestAdmin">{Template.bind()}</Story>
</Canvas>
