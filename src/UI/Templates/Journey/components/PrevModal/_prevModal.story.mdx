import { Meta } from "@storybook/addon-docs"

<Meta
  title="Templates/Journey/Journey Components/PrevModal"
  parameters={{
    viewMode: "docs",
    previewTabs: { canvas: { hidden: true } }
  }}
/>

# `PrevModal`

The `PrevModal` component checks the `formik`'s `dirty` property to see if there are any unsaved values and, if so, displays a `ConfirmationModal` to the user before allowing them to click the Previous `Button`.
