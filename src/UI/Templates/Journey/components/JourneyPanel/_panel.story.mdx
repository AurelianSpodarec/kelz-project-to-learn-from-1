import { Meta } from "@storybook/addon-docs"

<Meta
  title="Templates/Journey/Journey Components/JourneyPanel"
  parameters={{
    viewMode: "docs",
    previewTabs: { canvas: { hidden: true } }
  }}
/>

# `JourneyPanel`

The `JourneyPanel` is just a Journey dedicated `FlyOutPanel` that will get called as and when required and will be passed `body` and / or `wideBody` via the `JourneyProvider` to render any content.
